platform :ios, '10.0'

target 'Tibet' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tibet
  pod 'SwiftyJSON', '~> 5.0.0'
  pod 'Alamofire', '~> 4.8.2'
  pod 'SnapKit', '~> 4.2.0'
  
  target 'TibetTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TibetUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
